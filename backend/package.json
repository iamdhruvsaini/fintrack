{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is backend for the financial tracking portal",
  "repository": {
    "type": "git",
    "url": "fintrack"
  },
  "license": "MIT ",
  "author": "Dhruv Saini",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_ENV=development node src/server.js",
    "dev": "cross-env NODE_ENV=development nodemon src/server.js",
    "prod": "cross-env NODE_ENV=production node src/server.js",
    "db:migrate:prod": "cross-env NODE_ENV=production sequelize-cli db:migrate",
    "db:migrate:undo:prod": "cross-env NODE_ENV=production sequelize-cli db:migrate:undo",
    "db:migrate:status:prod": "cross-env NODE_ENV=production sequelize-cli db:migrate:status",
    "db:seed:prod": "cross-env NODE_ENV=production sequelize-cli db:seed:all",
    "db:seed:undo:prod": "cross-env NODE_ENV=production sequelize-cli db:seed:undo:all"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "sequelize-cli": "^6.6.5"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "connect-redis": "^9.0.0",
    "cors": "^2.8.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.0",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "http-status-codes": "^2.3.0",
    "morgan": "^1.10.1",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0",
    "pg": "^8.20.0",
    "pg-hstore": "^2.3.4",
    "redis": "^5.11.0",
    "sequelize": "^6.37.8"
  }
}
